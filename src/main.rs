fn main() {
    let spaces = "   ";
    println!("{spaces}");

    let spaces = spaces.len();
    println!("{spaces}");

    // 변수의 타입 변경 불가능. let 없이. 문자열 타입, 숫자 타입이었음
    // spaces = spaces.len();
    // println!("{spaces}");
}
